---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-mdns
  namespace: external-mdns
  annotations:
    kapp.k14s.io/change-group: external-mdns
    kapp.k14s.io/change-rule.0: "upsert after upserting metallb-ippool"
    kapp.k14s.io/change-rule.1: "delete before deleting metallb-ippool"
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: external-mdns
  template:
    metadata:
      labels:
        app: external-mdns
    spec:
      securityContext:
        runAsUser: 65534
        runAsGroup: 65534
        runAsNonRoot: true
      hostNetwork: true
      serviceAccountName: external-mdns
      containers:
      - name: external-mdns
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        image: blakec/external-mdns:latest
        args:
        - -source=ingress
        - -source=service